name: xgboost
type: tabular
family: baseline_tabular
representation_tag: current
params:
  n_estimators: 200
  max_depth: 4
  learning_rate: 0.05
  subsample: 0.9
  colsample_bytree: 0.9
  random_state: 42
